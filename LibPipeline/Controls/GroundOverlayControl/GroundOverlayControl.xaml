<wpfmap:MapPanel x:Class="LibPipeline.GroundOverlayControl"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                 xmlns:libpipeline="clr-namespace:LibPipeline"
                 xmlns:wpfmap="clr-namespace:MapControl;assembly=MapControl.WPF"
                 mc:Ignorable="d" 
                 d:DesignHeight="300" d:DesignWidth="300"
                 Name="Root">
    <wpfmap:MapImage Source="{Binding GroundOverlay.Icon.Href, ElementName=Root}"
                    North="{Binding GroundOverlay.Bounds.North, ElementName=Root}"
                    East="{Binding GroundOverlay.Bounds.East, ElementName=Root}"
                    South="{Binding GroundOverlay.Bounds.South, ElementName=Root}"
                    West="{Binding GroundOverlay.Bounds.West, ElementName=Root}" />

</wpfmap:MapPanel>
